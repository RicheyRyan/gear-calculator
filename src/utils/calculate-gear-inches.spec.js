import {
  should
} from 'chai';
should();

describe('Calculate gear inches', function() {
  it('should calculate the correct number of gear inches for a given wheel, cog and chainring', function() {
    console.log(should);
    const foo = 1;
    foo.should.equal(1);
  });
});
